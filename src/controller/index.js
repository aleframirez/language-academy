// Archivo barril de Controlers

// Users
const { getUsers, postUsers, updateUsers, deleteUsers } = require("./user.js");

module.exports = {
  // Users
  getUsers,
  postUsers,
  updateUsers,
  deleteUsers,
};
